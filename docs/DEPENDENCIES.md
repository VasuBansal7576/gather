# Dependency advisories

## PostCSS

Next 15.5.25 pins PostCSS 8.4.31, which is affected by four advisories covering style-output XSS and source-map file disclosure.
The advisories have differing severity and affected-version ranges; 8.5.23 clears the current audit findings.
The pinned `postcss: 8.5.23` npm override stays within PostCSS 8 and preserves the current Next version.
The worker verified a clean install, generated production CSS, type checking, and its complete 117-test suite.

## sharp

Next declares the optional sharp range `^0.34.3 || ^0.35.4`.
The lockfile now selects 0.35.4, which clears the current inherited image-library advisory findings.
The worker verified loading libvips 8.18.6 and encoding a generated PNG in memory.
The platform packages supply prebuilt binaries; sharp's unapproved installation script was not required for that check.

## Maintenance

The lockfile was regenerated by npm.
Direct dependency versions and the Next major version are unchanged.
The worker reported zero audit vulnerabilities after a clean install; integrated verification is recorded in [PROGRESS.md](PROGRESS.md).
